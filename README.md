# freesls

## Instalación

```bash
pnpm add freesls
# o
npm install freesls
```

## Scripts

- `build`: Compilar con TypeScript (`tsc`).
- `watch`: Compilar en modo escucha (`tsc --watch`).

## Licencia

[MIT](LICENSE)
